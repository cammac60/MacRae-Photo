import React from 'react';

import './Photo.scss';

export const Photo = props => {
  return (
    <div className="Photo">
      <div className="blur-wrapper-3">
      </div>
    </div>
  )
};

export default Photo;
